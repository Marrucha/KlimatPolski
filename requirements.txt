# === FETCH DANYCH ===
requests==2.31.0          # HTTP do NOAA ERDDAP
xarray==2023.12.0         # Parsowanie danych sieciowych (netCDF)
netCDF4==1.6.5            # Obsługa formatów meteorologicznych
numpy==1.26.0             # Operacje numeryczne, obliczenia wiatru
pandas==2.1.1             # Manipulacja danymi, agregacje

# === BAZA DANYCH ===
psycopg2-binary==2.9.9    # Adapter PostgreSQL do Pythona
sqlalchemy==2.0.23        # ORM dla Supabase
python-dotenv==1.0.0      # Zmienne środowiskowe (.env)

# === FIRESTORE (opcjonalnie dla cache) ===
firebase-admin==6.3.0     # SDK Firebase do Firestore

# === LOGGING I MONITORING ===
python-logging==0.5.1.2   # Logging strukturalny

# === DEV ===
black==23.11.0            # Formatowanie kodu
flake8==6.1.0             # Linting
mypy==1.7.0               # Type checking
pytest==7.4.3             # Unit testy
